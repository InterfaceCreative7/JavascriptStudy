1. 기본형과 참조형 데이터의 복사 비교
    var a = 10;
    var b = a;

    var obj1 = {c:10, d: 'ddd'};
    var obj2 = obj1;

    복사 과정은 동일하지만 데이터 할당과정에서 차이가 있기 때문에
    복사 이후의 동작에 큰 차이가 발생한다.

    위의 코드에,
    b = 15;
    obj2.c = 20;
    
    추가했을 경우